apiVersion: v1
kind: Service
metadata:
  name: service-app-urbanfood
  namespace: urbanfood
spec:
  ports:
  - port: 8000
    protocol: TCP
    targetPort: 8000
  selector:
    app: urbanfood
  type: LoadBalancer