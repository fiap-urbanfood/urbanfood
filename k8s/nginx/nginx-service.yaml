apiVersion: v1
kind: Service
metadata:
  name: service-nginx-urbanfood
  namespace: urbanfood
  labels:
    web: nginx-urbanfood
spec:
  ports:
  - name: service-nginx-http
    port: 80 
    protocol: TCP
    targetPort: 8080
  selector:
    web: nginx-urbanfood
  type: LoadBalancer
  